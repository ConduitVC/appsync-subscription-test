#if($context.identity.sub)
  #set($attributeValues=$ctx.args.quoteRequest)
  #set( $values = $attributeValues.put("customerId", "${context.identity.sub}") )
  {
    "version" : "2017-02-28",
    "operation" : "PutItem",
    "key": {
        "id" : $util.dynamodb.toDynamoDBJson($util.autoId())
    },
    "attributeValues": $util.dynamodb.toMapValuesJson($attributeValues)
  }
#else
  $utils.unauthorized()
#end
